import { GetIdByLinkPipe } from './get-id-by-link.pipe';

describe('GetIdByLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new GetIdByLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
